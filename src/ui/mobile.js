// Mobile UI initialisation.
// The sidebar-drawer pattern (open/close toggle + backdrop) has been superseded
// by the bottom-sheet in bottomsheet.js. This file is kept as a no-op so
// existing imports in main.js continue to resolve without errors.

export function initMobileUI() {
    // Bottom sheet (bottomsheet.js) and touch gestures (touch.js)
    // handle all mobile interactions. Nothing to do here.
}
